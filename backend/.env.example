# Port the Express server listens on.
PORT=5001

# Allowed CORS origin. Set this to the URL of the frontend application.
# In development this is typically http://localhost:3000.
CORS_ORIGIN=http://localhost:3000
